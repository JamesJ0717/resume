language: node_js
cache:
  directories:
    - ~/.npm
notifications:
  slack: jamesj0717:$SLACK_TOKEN
node_js:
  - "10"
git:
  depth: 3
script:
  - yarn build
deploy:
  provider: s3
  access_key_id: $AWS_KEY
  secret_access_key: $AWS_SECRET_KEY
  bucket: jj-resume.pdf
  skip_cleanup: true
  local_dir: build
